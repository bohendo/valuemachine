/*
export const getAccount = (address: string, ): string => {
  if (nativeAsset === Assets.ETH) return `evm:1:${getAddress(address)}`;
  if (nativeAsset === Assets.MATIC) return `evm:137:${getAddress(address)}`;
  // if (nativeAsset === Assets.ONE) return address; // use "oneAbC.." format instead of "0xabc.."
  return isAddress(address) ? getAddress(address) : address;
};
*/
