const { add, diff, div, eq, gt, lt, mul, round, sub } = require('./math');
const { emptyForm, mergeForms } = require('./forms');
const { parseHistory } = require('./parse-history');

module.exports = {
  add,
  diff,
  div,
  eq,
  gt,
  lt,
  mul,
  round,
  sub,
  emptyForm,
  mergeForms,
  parseHistory,
}
